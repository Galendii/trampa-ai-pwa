export type PlanModel = {
  id: string;
  createdAt: string;
  updatedAt: string;
  deletedAt: string;
  name: string;
  uuid: string;
  price: string;
  duration: number;
  frequency: number;
  active: boolean;
};
